#include <bits/stdc++.h>
int main()
{
    int n, arr[n];
    for (int i = 0; i < n; i++)
        cin >> arr[i];
    int k;
    cin >> k;

    int left=0,right=n-1,mid=(left+right)/2;
    if(arr[k]>left)

    return 0;
}